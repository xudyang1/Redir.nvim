*redir.nvim.txt*           For NVIM v0.8.0          Last change: 2024 April 03

==============================================================================
Table of Contents                               *redir.nvim-table-of-contents*

1. Redir                                                    |redir.nvim-redir|
  - Features                                       |redir.nvim-redir-features|
  - Requirements                               |redir.nvim-redir-requirements|
  - Installation                               |redir.nvim-redir-installation|
  - Configuration                             |redir.nvim-redir-configuration|
  - Usage                                             |redir.nvim-redir-usage|
  - Known Issues                               |redir.nvim-redir-known-issues|

==============================================================================
1. Redir                                                    *redir.nvim-redir*

Redirects neovim |cmdline| output to a modifiable buffer.


FEATURES                                           *redir.nvim-redir-features*

- uses a floating window to redirect cmdline output
- supports all `Ex` command
- keeps history for entire session including
    - command inputs
    - message outputs
- output window reopen
- customizable line separator


REQUIREMENTS                                   *redir.nvim-redir-requirements*

- Neovim >= 0.8.0
- Optional: a patched <https://www.nerdfonts.com/> font for better line separator visualization


INSTALLATION                                   *redir.nvim-redir-installation*

- lazy.nvim <https://github.com/folke/lazy.nvim>

>lua
    return {
        "xudyang1/redir.nvim",
        opts = {
          -- your configuration options...
          -- leave it empty to use the default settings
          -- refer to the configuration section below
        }
    }
<


CONFIGURATION                                 *redir.nvim-redir-configuration*


A COMPLETE LIST OF CONFIGURATION OPTIONS AND DEFAULT VALUES ~

>lua
    {
        -- TODO: add list
    }
<


USAGE                                                 *redir.nvim-redir-usage*


COMMANDS ~

Example keybindings:

>lua
    -- TODO: add examples
<


API ~

You can configure following functions in your keybindings

>lua
    -- TODO: add examples
<


KNOWN ISSUES                                   *redir.nvim-redir-known-issues*

If you use cmp-cmdline <https://github.com/hrsh7th/cmp-cmdline> to enable
autocompletion in cmdline mode, please note that you may experience input
hanging with `Redir !shellcmd`.

For example:

>txt
    :Redir !ls # <- input hanging may occur, press ctrl-c to kill the cmp-cmdline completion process
<

This is a known bug in cmp-cmdline
<https://github.com/hrsh7th/cmp-cmdline/issues/109>

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
